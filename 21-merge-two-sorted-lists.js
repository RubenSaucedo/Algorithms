// 21. mege two sorted lists - proposed solution

/**
 * input
 * Two *sorted* linked lists
 * 
 * output
 * A merged sorted linked list
 * 
 * Examples
 * Input: list1 = [1,2,4], list2 = [1,3,4]
 * Output: [1,1,2,3,4,4]
 * 
 * Breakdown solution
 *  Usually for merging problems, we 1. create a new DS or 2. override one
 *  Given they are sorted we should be able to use 2 pointers solution
 * 
 *  for 1.
 *      place the pointers in the head
 *      do value comparison
 */

import { ListNode } from './data-structures/linkedi-list-node'

var mergeTwoLists = function(list1, list2) {

}